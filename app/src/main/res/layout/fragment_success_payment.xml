<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SuccessPayment">

    <TextView
        android:id="@+id/textS"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Ваш заказ успешно обработан!"
        android:textColor="@color/lightBlueLogo"
        android:fontFamily="sans-serif-condensed"
        android:textSize="32sp"
        android:textAlignment="center"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/image_s"
        android:layout_width="160dp"
        android:layout_height="150dp"
        android:layout_marginTop="32dp"
        android:background="@drawable/n_success"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/textS" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/return_home"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:layout_marginTop="60dp"
        android:background="@drawable/btn_shape_lg"
        android:fontFamily="sans-serif-condensed"
        android:outlineAmbientShadowColor="@color/lightBlueLogo"
        android:outlineSpotShadowColor="@color/lightBlueLogo"
        android:shadowColor="@color/lightBlueLogo"
        android:text="Вернуться"
        android:textAllCaps="false"
        android:textColor="@color/lightBlueLogo"
        android:textSize="22dp"
        app:layout_constraintEnd_toEndOf="@+id/image_s"
        app:layout_constraintStart_toStartOf="@+id/image_s"
        app:layout_constraintTop_toBottomOf="@+id/image_s" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="80dp"
        app:layout_constraintTop_toBottomOf="@+id/return_home" />

</androidx.constraintlayout.widget.ConstraintLayout>